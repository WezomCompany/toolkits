{
  "name": "toolkits",
  "private": true,
  "scripts": {
    "----------------------------- Lerna": "***",
    "lerna:build": "lerna run build",
    "lerna:test": "npm run prettier && lerna run test",
    "lerna:test:autofix": "npm run prettier:fix && lerna run test:autofix",
    "lerna:upgrade": "lerna init",
    "lerna:bootstrap": "lerna bootstrap",
    "lerna:link": "lerna link convert",
    "lerna:publish": "lerna publish",
    "----------------------------- Prettier": "***",
    "prettier": "prettier ./{utils,packages} --check",
    "prettier:fix": "npm run prettier -- --write"
  },
  "devDependencies": {
    "@wezom/eslint-config": "^2.1.0-beta.0",
    "@wezom/eslint-config-ts": "^2.1.0-beta.0",
    "del-cli": "^3.0.1",
    "eslint": "^7.19.0",
    "eslint-config-prettier": "^7.2.0",
    "eslint-plugin-jest": "^24.1.3",
    "lerna": "^3.22.1",
    "prettier": "^2.2.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@wezom/toolkit-css-in-js": "file:packages\\css-in-js"
  }
}
